{
  "name": "testground",
  "version": "0.0.21",
  "private": true,
  "dependencies": {
    "@cra-express/core": "^2.3.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "cra-universal start --both",
    "serve": "NODE_ENV=production node ./server/bundle",
    "crau:start": "cra-universal start",
    "crau:build": "cra-universal build",
    "crau:clean": "cra-universal clean",
    "start:client": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prettier": "prettier --write \"{src,__{tests,mocks}__}/**/*.js\""
  },
  "devDependencies": {
    "@react-instudio/core": "^0.0.21",
    "cra-universal": "^3.2.1",
    "prettier": "1.13.3"
  }
}
